# RentalScout 计划

## 0. 项目目标

RentalScout 是一个面向个人租房决策的房源采集、验证、分析与交互式筛选系统。

第一目标不是“尽可能多地爬取房源”，而是建立一条可信的数据链路：

```text
数据源侦察 -> 抓取 -> 清洗 -> 合并 -> 验证 -> 分析 -> 交互式使用
```

早期重点是先让核心房源数据可信、可追溯、可抽样验证，再进入复杂分析和网站展示。

## 1. 数据源优先级

### 1.1 大房源平台

#### 贝壳租房

定位：大规模、结构相对稳定、覆盖面较广的房源来源。

优先抓取内容：

- 房源标题
- 租金
- 面积
- 户型
- 朝向
- 楼层
- 小区名
- 行政区 / 商圈
- 近地铁信息
- 发布时间 / 更新时间
- 房源详情页 URL
- 图片 URL
- 中介 / 品牌信息
- 页面抓取时间

### 1.2 个人出租 / UGC 房源

开发优先级：

```text
Wellcee > 豆瓣 > 小红书
```

第一阶段只正式开发 Wellcee。豆瓣和小红书先做可行性侦察，不进入完整工程实现。

#### Wellcee

定位：个人出租、转租、合租等更接近 UGC 的房源来源。

优先抓取内容：

- 帖子标题
- 正文描述
- 租金
- 地点 / 小区 / 区域
- 房型 / 合租类型
- 入住时间
- 联系方式线索
- 图片 URL
- 作者信息
- 发布时间
- 帖子 URL
- 页面抓取时间

#### 豆瓣

阶段 1 只做侦察：

- 小组 URL 与列表结构
- 是否需要登录
- 帖子字段稳定性
- 翻页机制
- 反爬风险
- 是否适合作为阶段 2 数据源

#### 小红书

阶段 1 只做侦察：

- 搜索结果是否稳定
- 是否强依赖登录态
- 是否强依赖 JS / App 环境
- 内容是否适合结构化成房源
- 是否适合作为 UGC 补充，而不是核心房源来源

## 2. 技术栈

### 2.1 基础运行环境

- Python 3.13
- uv

### 2.2 爬虫与解析

- Scrapy 2.16+
- scrapy-playwright
- Playwright Python
- parsel
- selectolax

使用原则：

- 普通 HTML 页面优先使用 Scrapy + parsel。
- 只有强 JS 渲染、异步加载或交互依赖页面才使用 Playwright。
- `selectolax` 可用于高性能 HTML 解析，但不要在同一解析模块里随意混用多种风格。

### 2.3 数据建模与清洗

- Pydantic v2
- Polars
- DuckDB

使用原则：

- Pydantic 用于解析结果 schema 和字段校验。
- Polars 用于批量清洗、字段标准化、重复检测前处理。
- DuckDB 用于本地分析、验证样本导出和中间数据检查。

### 2.4 持久化与空间分析

- PostgreSQL
- PostGIS

使用原则：

- 早期可以先用本地文件和 DuckDB 验证字段质量。
- 当数据结构稳定后，再将标准化房源、抓取记录和验证结果写入 PostgreSQL。
- PostGIS 主要服务于后续地图、通勤距离、地铁距离和区域聚合分析。

### 2.5 编排与质量

- Prefect 3
- Ruff
- pytest

使用原则：

- Ruff 和 pytest 从第一天启用。
- Prefect 等单站点爬虫脚本稳定后再接入，避免过早复杂化。

## 3. 阶段划分

### 阶段 0：数据源侦察

目标：确认每个数据源的可爬性、字段覆盖率、反爬风险和最小可行字段。

这一阶段不追求完整爬虫，只做技术侦察和小样本抓取。

#### 0.1 贝壳侦察

需要确认：

- 列表页 URL 模式
- 筛选参数是否可直接构造
- 分页机制
- 详情页 URL 结构
- 房源字段是否在 HTML 中直接存在
- 是否需要 Playwright
- 是否存在接口返回 JSON
- 是否存在明显反爬限制

#### 0.2 Wellcee 侦察

需要确认：

- 是否需要登录
- 列表页是否强依赖 JS
- 帖子详情页结构是否稳定
- 图片、正文、发布时间、作者信息是否可解析
- 是否有地区筛选或关键词搜索能力
- 是否适合增量抓取

#### 0.3 豆瓣 / 小红书侦察

只记录结论，不进入完整开发。

需要确认：

- 登录要求
- 反爬风险
- 数据结构稳定性
- 字段质量
- 后续开发优先级

#### 0.4 阶段输出

产出文件：

- `docs/source_research.md`

内容包括：

- 数据源名称
- 样例 URL
- 是否需要 Playwright
- 是否需要登录
- 关键字段覆盖率
- 反爬风险
- 推荐抓取频率
- 最小可行字段
- 是否进入阶段 1 开发

### 阶段 1：数据层 MVP

目标：实现贝壳 + Wellcee 的核心数据抓取、解析、清洗、合并、去重和本地存储。

阶段 1 不做复杂分析，不做完整交互式网站，只建立可信的数据基础。

#### 1.1 数据链路

核心流程：

```text
抓取原始数据 -> 解析 -> 校验 -> 标准化 -> 去重 -> 存储 -> 导出样本
```

#### 1.2 原始数据层

保留原始页面或原始响应，方便回溯解析错误。

建议结构：

```text
raw_pages
```

核心字段：

- `id`
- `source`
- `url`
- `fetched_at`
- `status_code`
- `content_type`
- `content_hash`
- `raw_path` 或 `raw_body`
- `request_meta`
- `error_message`

#### 1.3 解析结果层

每个数据源先解析成自己的来源专属 schema，然后再转换成统一 schema。

示例：

- `BeikeListingRaw`
- `WellceePostRaw`
- `NormalizedRentalListing`

这样可以避免不同平台字段差异过早污染统一模型。

#### 1.4 标准化房源主表

建议结构：

```text
rental_listings
```

核心字段：

- `id`
- `source`
- `source_listing_id`
- `source_url`
- `title`
- `description`
- `rent_price`
- `rent_price_unit`
- `area_sqm`
- `layout`
- `district`
- `subdistrict`
- `community_name`
- `address_text`
- `longitude`
- `latitude`
- `floor`
- `orientation`
- `available_from`
- `published_at`
- `updated_at`
- `first_seen_at`
- `last_seen_at`
- `listing_type`
- `landlord_type`
- `contact_hint`
- `image_urls`
- `raw_record_id`
- `parse_confidence`

#### 1.5 观察记录表

租房数据变化很快，不应只保存当前状态。

建议结构：

```text
listing_observations
```

核心字段：

- `id`
- `listing_id`
- `source`
- `observed_at`
- `rent_price`
- `availability_status`
- `title`
- `description_hash`
- `image_hash`
- `raw_record_id`

用途：

- 记录价格变化
- 记录房源是否下架
- 记录重复出现
- 为后续分析提供历史数据

#### 1.6 去重策略

阶段 1 先做保守去重，不做激进合并。

第一层：同源去重

- 相同 `source` + `source_listing_id`
- 相同 `source` + `source_url`
- 相同 `source` + `title` + `rent_price` + `community_name`

第二层：跨源候选重复

- 小区名相似
- 租金接近
- 面积接近
- 标题 / 描述相似
- 图片相似
- 地理位置接近

跨源重复在阶段 1 只标记候选关系，不直接合并成一个房源。

建议结构：

```text
listing_duplicate_candidates
```

核心字段：

- `listing_id_a`
- `listing_id_b`
- `score`
- `reason`
- `created_at`
- `review_status`

#### 1.7 阶段输出

阶段 1 完成后应能做到：

- 稳定抓取贝壳核心房源
- 稳定抓取 Wellcee 核心帖子
- 保存原始页面
- 解析成结构化字段
- 统一成标准房源 schema
- 进行基础去重
- 导出抽样数据
- 为验证层提供样本

### 阶段 2：验证层

目标：确认抓取和解析出的数据是否可信。

验证层应在分析层之前完成。否则后续分析会被错误字段污染。

#### 2.1 人工验证

每次抓取后随机抽样一批房源，由人工对照真实网页检查。

建议每轮抽样：

- 贝壳 20-50 条
- Wellcee 20-50 条

验证字段：

- 标题是否正确
- 租金是否正确
- 面积是否正确
- 地点 / 小区是否正确
- 发布时间是否正确
- 房源 URL 是否有效
- 图片是否对应
- 是否已下架

#### 2.2 半自动验证材料

生成验证材料，减少人工检查成本。

可以输出：

- CSV
- DuckDB 查询结果
- 简单 HTML 验证页

每条验证样本包含：

- 抓取字段
- 原站链接
- 原始页面路径
- 页面截图路径
- 解析置信度
- 验证状态
- 验证备注

#### 2.3 验证结果表

建议结构：

```text
validation_reviews
```

核心字段：

- `id`
- `listing_id`
- `source`
- `reviewed_at`
- `reviewer`
- `title_status`
- `price_status`
- `area_status`
- `location_status`
- `image_status`
- `availability_status`
- `overall_status`
- `notes`

#### 2.4 验证指标

需要量化：

- 标题正确率
- 租金正确率
- 面积正确率
- 位置正确率
- 图片正确率
- URL 有效率
- 下架识别准确率
- 解析失败率
- 重复候选准确率

#### 2.5 阶段通过标准

进入分析层前，至少满足：

- 核心字段正确率达到可接受水平
- 解析失败原因可定位
- 随机样本验证流程稳定
- 原始数据可回溯
- 明显重复和明显错误可以被标记

### 阶段 3：分析层

目标：在验证通过后，基于可信数据做租房决策分析。

阶段 3 不再讨论“能不能抓”，而是研究“这些房源对租房决策有什么价值”。

#### 3.1 初始分析方向

优先分析：

- 区域租金分布
- 同小区 / 相近位置价格差
- 单位面积租金
- 地铁距离与租金关系
- 通勤时间与租金关系
- 房源新鲜度
- 个人房源与中介房源差异
- 异常低价可疑房源
- 重复发布 / 搬运房源

#### 3.2 空间分析

空间分析以高德地图 API + PostGIS 为核心。

高德地图 API 主要用于：

- 地址解析 / 逆地址解析
- 工作地点坐标确认
- 房源地址坐标补全
- 路线规划
- 通勤时间估算
- 基于通勤时间的可达范围判断

PostGIS 主要用于：

- 地铁站距离
- 商圈距离
- 指定地点通勤圈
- 区域聚合
- 地图边界筛选

核心分析模型：

```text
工作地点 -> 通勤时间阈值 -> 可达房源集合
```

其中工作地点作为目标点，通勤时间作为筛选半径或等时圈约束。系统应支持按不同通勤方式计算，例如：

- 步行
- 骑行
- 公共交通
- 驾车

第一版可以先用高德路线规划 API 判断单个房源到工作地点的通勤时间，后续再研究是否生成真正的等时圈边界。

需要保存的通勤分析结果：

- `listing_id`
- `workplace_id`
- `transport_mode`
- `duration_minutes`
- `distance_meters`
- `amap_route_raw_id` 或原始响应引用
- `calculated_at`

注意：高德返回结果应作为可更新的分析结果，不应覆盖原始房源数据。

#### 3.3 房源评分

后续可设计一个可解释的评分系统。

候选维度：

- 租金
- 面积
- 地点
- 通勤
- 新鲜度
- 来源可信度
- 是否个人房源
- 是否疑似重复
- 是否验证通过

评分必须保留解释原因，不能只给一个黑盒分数。

### 阶段 4：交互式网站

目标：把数据和分析结果变成可用于实际租房决策的交互式工作台。

网站不是展示爬虫成果，而是帮助选择、比较、排除和收藏房源。

#### 4.1 核心功能

- 房源列表
- 地图视图
- 以工作地点为目标点的通勤范围筛选
- 通勤时间阈值筛选
- 通勤方式筛选
- 租金筛选
- 面积筛选
- 区域筛选
- 来源筛选
- 验证状态筛选
- 分析评分排序
- 收藏房源
- 排除房源
- 多房源对比
- 原站链接跳转

#### 4.2 房源详情页

详情页展示：

- 标准化字段
- 原始来源
- 抓取时间
- 验证状态
- 分析评分
- 评分解释
- 历史价格变化
- 可能重复房源
- 原站链接

#### 4.3 决策辅助

后续可以增加：

- 我的工作地点 / 常去地点
- 通勤时间估算
- 通勤圈内房源展示
- 超出通勤阈值的房源自动降权或隐藏
- 预算约束
- 偏好权重
- 更多筛选条件
- 对比候选集
- 看房记录
- 排除原因

### 阶段 5：UGC 内容增强

目标：在核心结构化房源稳定后，引入更复杂的 UGC 内容。

UGC 不应在阶段 1 直接扩大范围。它应作为增强层进入。

#### 5.1 UGC 内容范围

可能包括：

- Wellcee 正文语义
- 豆瓣帖子正文
- 小红书笔记正文
- 评论内容
- 作者可信度线索
- 联系方式线索
- 图片内容线索
- 语气和风险提示

#### 5.2 UGC 分析方向

候选方向：

- 是否个人转租
- 是否中介伪装
- 是否短租
- 是否合租
- 是否急租
- 是否有明显风险词
- 是否描述不完整
- 是否与结构化字段矛盾

#### 5.3 UGC 风险

需要特别处理：

- 登录态和账号风险
- 平台规则
- 个人隐私
- 联系方式敏感信息
- 内容质量不稳定
- 文本难以结构化

## 4. 工程结构建议

初始目录可以设计为：

```text
RentalScout/
  plan.md
  pyproject.toml
  README.md
  docs/
    source_research.md
    validation_protocol.md
  rentalscout/
    __init__.py
    settings.py
    schemas/
      raw.py
      normalized.py
      validation.py
    spiders/
      beike.py
      wellcee.py
    parsers/
      beike.py
      wellcee.py
    pipelines/
      raw_store.py
      normalize.py
      dedupe.py
    storage/
      duckdb.py
      postgres.py
    validation/
      sample.py
      report.py
    analysis/
      pricing.py
      geo.py
  tests/
    test_parsers/
    test_schemas/
    test_dedupe/
```

## 5. 近期执行顺序

### 步骤 1：初始化工程

- 创建 `pyproject.toml`
- 配置 Python 3.13
- 配置 uv
- 配置 Ruff
- 配置 pytest
- 建立基础包结构

### 步骤 2：数据源侦察

- 侦察贝壳
- 侦察 Wellcee
- 记录豆瓣 / 小红书初步风险
- 写入 `docs/source_research.md`

### 步骤 3：贝壳最小爬虫

- 抓取一个城市 / 一个筛选条件
- 保存原始页面
- 解析列表页
- 解析详情页
- 输出标准化样本

### 步骤 4：Wellcee 最小爬虫

- 抓取一个城市 / 一个关键词或列表
- 保存原始页面
- 解析帖子
- 输出标准化样本

### 步骤 5：统一 schema 与本地存储

- 定义 Pydantic schema
- 写入 DuckDB
- 导出样本 CSV
- 记录失败样本

### 步骤 6：验证样本生成

- 随机抽样
- 生成验证 CSV 或 HTML
- 人工标记结果
- 汇总字段正确率

## 6. 第一阶段完成标准

第一阶段完成时，应满足：

- 贝壳和 Wellcee 都有可重复运行的最小爬虫
- 原始数据可保存并回溯
- 解析结果通过 Pydantic 校验
- 标准化房源可写入本地存储
- 能导出验证样本
- 能记录人工验证结果
- 能统计字段正确率
- 豆瓣和小红书已有侦察结论，但未进入完整开发

## 7. 暂不做的事情

为了控制范围，第一阶段暂不做：

- 完整交互式网站
- 复杂推荐算法
- LLM 语义分析
- 小红书完整爬虫
- 豆瓣完整爬虫
- 自动联系房东
- 自动登录账号池
- 激进反爬绕过
- 大规模分布式爬取

## 8. 关键原则

- 先可信，再全面。
- 先保留原始数据，再优化解析。
- 先人工验证，再做分析。
- 先贝壳和 Wellcee，再扩展豆瓣和小红书。
- 先本地可运行，再考虑复杂编排。
- 跨源去重要保守，宁可先标记候选，不要误合并。
- 分析结果必须可解释，尤其是评分和风险判断。
